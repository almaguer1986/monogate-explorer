// Local mirror of the monogate library for the explorer.
// After `npm publish`, replace this with: import { ... } from "monogate";
export { op, exp, ln, E, ZERO, NEG_ONE, sub, neg, add, mul, div, pow, recip, IDENTITIES }
  from "../../lib/src/index.js";
